## Root .env (monorepo) — single place for configuration
# Copy this file to .env at the repo root and fill in real values.
# Both API and Web read from this file automatically (via dotenv in API and next.config).

# --- Common ---
NODE_ENV=development

# --- API (apps/api) ---
# Port where the Express API runs locally
PORT=3001

# JWT secret for signing tokens (change this in production!)
JWT_SECRET=change-me

# PostgreSQL connection string (Supabase or any Postgres)
# Example (Supabase):
# DATABASE_URL=postgresql://postgres:<password>@<host>:5432/postgres?sslmode=require
DATABASE_URL="postgresql://postgres:Shiv2005%40..@db.elbiuelotvxkjjwipcmg.supabase.co:5432/postgres?sslmode=require"

# Optional: Direct URL for Prisma (admin role). Usually not required.
# DIRECT_URL=

# Optional services
REDIS_URL=redis://localhost:6379
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Supabase (optional for client SDK usage from server)
SUPABASE_URL="https://elbiuelotvxkjjwipcmg.supabase.co"
SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVsYml1ZWxvdHZ4a2pqd2lwY21nIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTk2MDA2NTQsImV4cCI6MjA3NTE3NjY1NH0.XM4H3LnNMQdgQ1cbtK83wlyNKJXfH4ZpHeja4r8Gs6I"

# SMTP for OTP emails (recommended). For Gmail, use an App Password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="QSTEEL <noreply@qsteel.test>"

# Email feature flags
# Set DISABLE_EMAIL=0 to actually send emails; keep 1 for dev without SMTP
DISABLE_EMAIL=1
OTP_DEV_LOG=1
OTP_DEBUG=0
SMTP_DEBUG=0

# External ML service (optional)
ML_URL=

# --- Web (apps/web) ---
# Base URL the browser should call for API and Socket.IO
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
