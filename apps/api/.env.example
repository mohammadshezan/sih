# API Environment Variables (.env)

# Server
PORT=4000
JWT_SECRET=change-me

# Database (optional)
DATABASE_URL=postgresql://qsteel:qsteel@localhost:5432/qsteel?schema=public

# Redis (optional)
REDIS_URL=redis://localhost:6379

# CORS (optional). Comma-separated list of allowed origins. Leave empty to allow all (for demos only).
# Example: https://qsteel-web.vercel.app,https://admin.qsteel.com
CORS_ORIGINS=

# External ML/Forecast service (optional)
# Base URL without trailing slash. Endpoint /forecast will be called.
# Example: https://qsteel-ml.onrender.com
ML_URL=

# SMTP for OTP emails
# For Gmail, create an App Password (2FA required) and paste it below.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=qsteel.hitk@gmail.com
SMTP_PASS=your_gmail_app_password
SMTP_FROM="QSTEEL <qsteel.hitk@gmail.com>"

# Development seed key (optional)
# If set, /dev/seed-demo will require X-Seed-Key header to run in production
# SEED_KEY=change-me

# Development convenience: return OTP code in JSON instead of sending email
# DISABLE_EMAIL=1
