# Web app env configuration for Vercel/Netlify/etc
# Set the API base URL (your Express API or any HTTP endpoint)
NEXT_PUBLIC_API_URL=
# Optionally set a dedicated Socket.IO URL (falls back to API_URL when blank)
NEXT_PUBLIC_SOCKET_URL=
